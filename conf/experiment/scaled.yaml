# @package _global_
defaults:
  - /meta_dataset@train_meta_dataset: survival_dist_prior
  - _self_
  - /callbacks@callbacks.checkpoint: checkpoint # add the checkpointing callback
  - /callbacks@callbacks.eval_survival: eval_survival # add the evaluation of the survival model
  - override /model@model: tabdpt_long_context_scaled # use a fresh scaled model

train_meta_dataset:
  device: cpu
  n_samples: 2048
